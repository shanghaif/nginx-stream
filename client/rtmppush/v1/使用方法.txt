1. Telnet到设备中，将本文件夹放到/root下，执行./install.sh，然后重启设备；

2. Telnet到设备中，进入到/root下，执行./test 2 rtmp://192.168.2.3:1935/live
	说明：第一个参数指定从哪个buffer取流，2表示buffer C，它的分辨率固定为640*480，码率为500K，帧率为20；
		第二个参数为RTMP服务器的地址。

3. 在VLC中输入rtmp://192.168.2.3:1935/live即可看到视频，我在公司搭服务器测试到的延时为5秒。